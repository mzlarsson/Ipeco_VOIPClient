package se.chalmers.fleetspeak.test;

import java.net.DatagramPacket;
import java.net.DatagramSocket;

public class UDPBot {

	private DatagramSocket out;
	
	public UDPBot() {
//		out = new DatagramSocket(675);
//		out.send(new DatagramPacket(arg0, arg1, arg2, arg3));
		//out = new DatagramSocket(port, laddr);
	}
}
