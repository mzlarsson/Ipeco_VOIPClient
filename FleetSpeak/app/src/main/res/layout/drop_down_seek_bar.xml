<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="25dp">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="20dp"
        >
    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:id="@+id/volume_image"
        android:src="@drawable/ic_volume_orignal"
        android:layout_toLeftOf="@+id/volume_seekbar"/>
    <ImageView
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginTop="20dp"
        android:layout_marginRight="10dp"

        android:id="@+id/mic_image"
        android:src="@drawable/ic_mic_original"
        android:layout_toLeftOf="@+id/mic_seekbar"
        android:layout_below="@id/volume_image"/>
   <SeekBar
       android:id="@+id/mic_seekbar"
       android:layout_width="200dp"
       android:layout_marginTop="30dp"
       android:layout_height="wrap_content"
       android:layout_below="@id/volume_seekbar"
       android:layout_centerHorizontal="true"/>
    <SeekBar
        android:id="@+id/volume_seekbar"
        android:layout_width="200dp"
        android:layout_marginTop="10dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"/>
    </RelativeLayout>

</FrameLayout>